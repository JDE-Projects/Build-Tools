# Dependabot keeps the SHA-pinned Actions in release.yml up to date.
# It opens a PR (which you review) when a newer release of an action exists,
# updating both the pinned SHA and its version comment.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
